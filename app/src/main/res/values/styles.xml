<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeNoActionBar" parent="Theme.Design.Light.NoActionBar" />

    <style name="formLayoutContent">
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <style name="formLabel">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/label_color</item>
    </style>

    <style name="formEditText">
        <item name="android:paddingBottom">20dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:textColor">@color/et_color</item>
        <item name="android:textColorHint">@color/et_hint_color</item>
    </style>

    <style name="FormCheckBox" parent="Base.Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:colorControlNormal">@color/cb_color</item>
        <item name="android:colorControlActivated">@color/cb_color</item>
        <item name="android:colorControlHighlight">#c51162</item>
        <item name="android:checkMarkTint">@color/cb_check_color</item>
    </style>

    <style name="formButton">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/btn</item>
    </style>

    <style name="formCheckBox">
        <item name="android:background">@color/cb_color</item>
    </style>
</resources>
